<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mydesignapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/MyTheme">
        <activity android:name=".releasejob.releasejob4.ReleaseJob4Activity"></activity>
        <activity android:name=".releasejob.releasejob3.ReleaseJob3Activity" />
        <activity android:name=".releasejob.releasejob2.ReleaseJob2Activity" />
        <activity
            android:name=".releasejob.releasejob1.ReleaseJob1Activity"
            android:label="@string/title_activity_release_job"
            android:theme="@style/MyTheme.NoActionBar" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>